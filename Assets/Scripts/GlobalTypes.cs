namespace GlobalTypes
{
    public enum GroundType
    {
        None,
        LevelGeometry,
        OneWayPlatform,
        MovingPlatform,
        CollapsiblePlatform,
        JumpPad
    }

    public enum WallType
    {
        None,
        Normal,
        Sticky
    }
}